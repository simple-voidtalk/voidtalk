# Voidtalk Monorepo

## Installation

```bash
pnpm install
```

```bash
pnpm run dev
```

## Adding components to @voidtalk/ui Package

replace (component) with the name of the shadcn component 

```bash
pnpm dlx shadcn@latest add (component) -c apps/main
```